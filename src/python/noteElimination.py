def noteElimination(N, D):

sum = 0;
i = 1;

for x in range(1, len(N)):
  sum = sum + N(x)

minAvgNoteEnergy = sum/len(N)

for y in range(1, len(N))
	if (N(j) < minAvgNoteEnergy):
		E(i) = N(y)
	else:
		C(i) = N(y)
		i=i+1


sort(D)
minTrajLen = D(1)

for j in range(1, len(N))
	if D(j) < minTrajLen:
		E(end+1) = N(j)
	else:
		C(end+1) = N(j)
		i=i+1

sort(N)
for z in range(1, len(N))
	for a in range(z+1, len(N))
		if (N(j) < N(i)*.5:
			C(end+1) = N(z)
			E(end+1) = N(z)
